terraform {
  backend "s3" {
    bucket             = "STATEBUCKETNAME"
    encrypt            = true
    key                = "terraformstates/AirlyAlexaMainFunction.tfstate"
    dynamodb_table     = "TABLENAME"
    region             = "REGION"
  }
}